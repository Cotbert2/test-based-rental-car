class CarAvailability {
    constructor(id, car_id, location_id, available_from, available_to) {
        this.id = id;
        this.car_id = car_id;
        this.location_id = location_id;
        this.available_from = available_from;
        this.available_to = available_to;
    }
}